# 继承 onlight
inherits = "onelight"

"ui.virtual.inline-blame" = { fg = "grey" }

"attribute" = { fg = "pink" }
"special" = { fg = "blue" }

# 使用 term 默认的背景色，提供 opacity 透明功能
# "ui.background" = { }
"ui.background" = { bg = "white"}

"markup.list.checked" = { fg = "green", modifiers = ["bold"]}
"markup.list.unchecked" = { fg = "blue", modifiers = ["bold"]}
